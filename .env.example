# Inventory scraper configuration
# Copy to .env and fill in real values. Do NOT commit .env.

# ---- Vercel runtime (real-time stock check API) ----
# Set both on Vercel project settings, NOT here. The worker URL is the
# public address of your Lightsail server.
# WORKER_URL=http://13.124.xxx.xxx:8080
# WORKER_TOKEN=<same token as worker/.env>

# ---- Local PoC scraper (only used by scripts/win/scrape-poc.bat) ----

# Comma-separated adapter keys to run (must exist in src/scrapers/adapters/index.ts)
SCRAPER_SITES=ibjp

# Per-site rate limit between requests (ms). 2000ms = safe default.
SCRAPE_INTERVAL_MS=2000

# Distribution strategy: round-robin | cross
#   round-robin: each code hits one site (fast, load-split across sites)
#   cross:       each code hits every site (slow, enables cross-site price compare)
SCRAPE_DISTRIBUTION=round-robin

# Optional cap on codes per run (useful for smoke tests)
# SCRAPE_LIMIT=50

# Credentials per site — pattern: SCRAPER_<KEY>_ID / _PW
SCRAPER_IBJP_ID=
SCRAPER_IBJP_PW=

# Placeholders for additional sites (up to 5 total):
# SCRAPER_SITE2_ID=
# SCRAPER_SITE2_PW=
# SCRAPER_SITE3_ID=
# SCRAPER_SITE3_PW=
# SCRAPER_SITE4_ID=
# SCRAPER_SITE4_PW=
# SCRAPER_SITE5_ID=
# SCRAPER_SITE5_PW=

# Kakao JavaScript SDK — 제약사별 일괄제출 탭에서 카카오톡 공유 기능 활성화
# https://developers.kakao.com 에서 앱 생성 후 JavaScript 키를 입력
NEXT_PUBLIC_KAKAO_APP_KEY=

# Coolsms SMS 인증 — 회원가입 휴대폰 본인인증
# https://console.coolsms.co.kr 에서 API 키 발급, 발신번호는 콘솔에서 등록 필요
COOLSMS_API_KEY=
COOLSMS_API_SECRET=
COOLSMS_SENDER=

# 카카오 알림톡 채널 — Coolsms 콘솔 > 카카오 채널 등록에서 확인
KAKAO_PF_ID=
KAKAO_TEMPLATE_FILTER_REQUEST=
KAKAO_TEMPLATE_FILTER_RESULT=

# Naver Clova OCR — 처방전 1차 OCR
CLOVA_OCR_INVOKE_URL=
CLOVA_OCR_SECRET_KEY=

# Google Gemini API — 처방전 Vision OCR + LLM 검증/병합 + 카카오 실적 자동기록
GEMINI_API_KEY=

# ---- Google Sheets / Drive 서비스 계정 (실적 기록 + 데이터 동기화 공용) ----
# Google Cloud Console → IAM → 서비스 계정 생성 → 키 발급(JSON).
# JSON 안의 client_email / private_key 를 그대로 옮긴다. (private_key 의 \n 은 그대로 둘 것)
GOOGLE_DRIVE_CLIENT_EMAIL=
GOOGLE_DRIVE_PRIVATE_KEY=
# 시트를 둘 구글 드라이브 폴더 ID (드라이브에서 폴더 우클릭 → 링크 복사, /folders/<ID> 부분).
# 서비스 계정 이메일을 이 폴더의 "편집자" 로 공유해야 시트 생성 가능.
GOOGLE_DRIVE_FOLDER_ID=

# ---- 병원 실적 카카오톡 → Gemini → Google Sheets 파이프라인 ----
# docs/GEMINI_SALES_EXTRACTION.md 참고
# 실적을 기록할 구글 스프레드시트 이름. 위 폴더 안에 자동 생성/재사용.
GOOGLE_SHEETS_SALES_SPREADSHEET_NAME=병원 실적 데이터
# 처방 통계 표(EMR 화면) 다행 추출 결과를 기록할 스프레드시트 이름.
# 같은 폴더 안에 자동 생성. 두 탭 (요약 / 약품) 으로 구성됨.
GOOGLE_SHEETS_RX_STATS_SPREADSHEET_NAME=처방통계 데이터
# 카카오 i 오픈빌더 스킬 서버 호출 인증 토큰.
# 충분히 긴 랜덤값 권장: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# 카카오 콘솔 → 스킬 → 헤더에 X-Kakao-Token 으로 전달하거나 URL 끝에 ?token=<값> 으로 붙인다.
KAKAO_SALES_WEBHOOK_SECRET=

# 텔레그램 일일 디제스트 봇 — docs/TELEGRAM_SETUP.md 참고
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID_BIZ=
TELEGRAM_WEBHOOK_SECRET=
CRON_SECRET=

# ---- ePharms 매출원장 자동수집 ----
# 워커(Lightsail)와 통신용. 워커의 /epharms/sync 엔드포인트를 호출.
WORKER_BASE_URL=http://13.124.xxx.xxx:8080
# (WORKER_TOKEN 은 위 인벤토리 워커 토큰을 그대로 재사용)

# AES-256-GCM 키 (32바이트). hex(64자) 또는 base64로.
# 생성: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# ⚠️ 이 키가 유출되면 모든 거래처의 ePharms PW가 복호화 가능 — Vercel/워커 양쪽 동일값 필요.
EPHARMS_ENC_KEY=
