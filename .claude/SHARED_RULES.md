# 모든 BIZ 에이전트 공통 룰

> 사용자 명시 — 위반 시 DOMAIN_VIOLATION 보고 + 즉시 중단

---

## ⏱ 실시간 관제 — ACTION_LOG.md 기록 의무 (모든 에이전트)

작업 시작·진행·완료 시 `.claude/ACTION_LOG.md` 표 상단에 한 줄 추가.
형식:
```
| YYYY-MM-DDTHH:MM:SSZ | 에이전트명 | START/PROGRESS/DONE/FAIL | 작업명 | 진행% | 현재파일 | 추정토큰 | 모델티어 |
```
사용자는 이 파일 1개로 전체 관제. 누적 토큰도 갱신.

---

## 💸 모델 3티어 사용 룰 (토큰 절약)

| 티어 | 모델 | 누가 | 무엇을 |
|---|---|---|---|
| **OPUS** | Opus 4.7 | PM(메인) 전용 | 라우팅·검증·종합 보고·복잡 설계 결정 |
| **SONNET** | Sonnet 4.6 | Dev1~7 | 페이지·API·DB 모델 구현, 비자명한 로직 |
| **HAIKU** | Haiku 4.5 | 노가다 전담 | 주석·README·단위 테스트·SQL 마이그레이션 정리·문서 정정·grep 진단 |

PM은 작업 디스패치 시 적정 티어 선택 의무. 단순 grep/주석 추가에 SONNET/OPUS 호출 금지.

---

## 🚦 승인 게이트 (사용자 사전 OK 필수)

다음은 자율 진행 금지, 사용자 컨펌 후 진행:
- 추정 100k 토큰 초과 단일 작업 (전체 리팩토링, 대규모 라이브러리 설치, 5만 라인+ 변경)
- 외부 결제·발송 (Vercel deploy, npm publish, 사이트 운영 외부 호출)
- 데이터 손실 가능 작업 (DROP TABLE, force push, 마이그레이션 다운그레이드)

위 외엔 자율 진행 + 일일 9시 KST 텔레그램 보고로 누적.

---

## 📦 컨텍스트 압축

- 작업 완료 시 결과는 ACTION_LOG에 한 줄로 압축 (긴 산출물은 commit message로 보존)
- 다음 라운드 시작 시 이전 라운드 산출물은 git diff·로그·PROGRESS.md로 참조
- 메인은 백그라운드 에이전트 결과 받으면 핵심만 추출 → ACTION_LOG 갱신

---

## 데이터 마스터 (절대 룰)

### 거래처 등록처는 단 2곳
- **병의원** = `/biz/clients` (모델: `Client`, `UserClient`)
- **법인 / 딜러** = `/biz/dealers` (비-병의원 모든 사업자, 모델: `UserClient` with `dealerType`)

### 다른 모든 페이지는 읽기만
- 통계제출처, 정산 업로드, 요율, 필터링 등 다른 메뉴는 위 두 곳의 데이터를 **참조**만 함
- 자체 거래처/업체를 직접 추가하는 UI 만들지 말 것
- 분류·플래그·태그가 필요하면 마스터 모델에 컬럼 추가 (예: `isSettlementTarget`, `isRateTarget`)
- 이렇게 하면 데이터가 한 곳에서만 관리되어 꼬일 일이 없음

### 유저 = 3종 한정
- 병의원 / 법인 / 딜러
- 영업사원은 `User.role` 단위 (별도 거래처 X)

## 자율 진행 모드

- 사용자에게 매번 묻지 않음. 메인 추천안 기본값으로 진행
- 합리적 기본값: ORCHESTRATION.md §5 참고
- 커밋 메시지에 "사용자 자율결정 항목" 명시 (나중에 사용자가 보고 수정 요청 가능)

## 검증 게이트 (절대 스킵 금지)

- verify → biz-qa-crosscheck PASS → implement → biz-qa-crosscheck PASS → 커밋
- QA가 FAIL 주면 작성자 에이전트에 재작업 지시. 자체 무시 금지

## 메뉴 이름 표기 (확정)

- 거래처/유저 관리
- 통계제출처 관리
- 요율 관리
- 정산 관리
- 필터링 관리

## 스타일

- "매핑없음" → "컬럼 매핑 없음" 처럼 명시적으로
- 사용자가 무슨 말인지 모를 단어 쓰지 말기 (오버라이드, 시뮬레이션 등 사용 시 옆에 한글 풀이)
